{
  "name": "bot-farm",
  "version": "1.0.0",
  "main": "index.js",
  "author": "nyamadori <nyamadorig@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "dev-deploy": "npm run build && lambda-deploy development exec-bot",
    "prod-deploy": "npm run build && lambda-deploy production exec-bot"
  },
  "devDependencies": {
    "@types/node": "^8.0.24",
    "awesome-typescript-loader": "^3.2.3",
    "typescript": "^2.4.2",
    "webpack": "^3.5.5"
  },
  "dependencies": {
    "@slack/client": "^3.11.0",
    "aws-sdk": "^2.102.0"
  }
}
